# import json

# # user_model = UserModel()  # create an instance of UserModel
# # json_str = json.dumps(user_model.users)  # serialize the users attribute

# class UserModel():
#     def __init__(self):
#         users_json = [
#             {"username": "admin", "password": "admin", "admin": True},
#             {"username": "user", "password": "user", "admin": False},
#         ]
#         self.users = json.loads(users_json)

# def get_user(self, username, password):
#         for user in self.users:
#             if user["username"] == username and user["password"] == password:
#                 return user
#         return None

